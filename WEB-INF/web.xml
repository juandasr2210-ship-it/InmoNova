<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <display-name>InmoNova</display-name>

    <!-- Cadena de conexion CENTRALIZADA (no repetir en cada JSP).
         Se lee desde includes/conexion.jspf con application.getInitParameter -->
    <context-param>
        <param-name>db.url</param-name>
        <param-value>jdbc:mysql://localhost:3306/inmonova?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true&amp;useUnicode=true&amp;characterEncoding=UTF-8</param-value>
    </context-param>
    <context-param>
        <param-name>db.user</param-name>
        <param-value>root</param-value>
    </context-param>
    <context-param>
        <param-name>db.pass</param-name>
        <param-value></param-value>
    </context-param>
    <context-param>
        <param-name>db.driver</param-name>
        <param-value>com.mysql.cj.jdbc.Driver</param-value>
    </context-param>

    <!-- ============ CONEXION EN LINEA (Neon PostgreSQL) ============
         1) Importa sql/05_neon_postgres.sql en el SQL Editor de Neon.
         2) Copia postgresql-42.7.x.jar a C:\xampp\tomcat\lib y reinicia Tomcat.
         3) Reemplaza los 3 valores de arriba por estos (driver org.postgresql.Driver):
         db.url  = jdbc:postgresql://TU_ENDPOINT.neon.tech:5432/inmonova?sslmode=require
         db.user = TU_USUARIO_NEON
         db.pass = TU_CLAVE_NEON
         Para volver a local, restaura los valores MySQL. El codigo JSP
         es portable y funciona en ambas sin cambios. -->

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Sprint 1: 100% JSP (exigencia docente). Sin servlets ni filtros Java.
         El control de acceso por rol se hace en servidor con scriptlets
         en includes/seguridad.jspf + verificacion al inicio de cada dashboard. -->

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <error-page>
        <error-code>404</error-code>
        <location>/acceso-denegado.jsp</location>
    </error-page>
</web-app>
